# Local secrets — never commit
.env

# User records (dev store)
users.json

# Pending signup + 5-digit code + reset-token stores
pendings.json
codes.json
resets.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
